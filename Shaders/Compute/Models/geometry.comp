struct Material
{
    vec4 emissionColor;
    vec4 baseColor;
    
    int reflectance;
    int roughness;
    int metallic;

    // This is only here to make the material size divisable by 4 to work with std430 rules
    int padding;
};

struct Geometry
{
    mat4 transform;
    Material material;
};